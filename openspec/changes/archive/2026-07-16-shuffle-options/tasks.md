## 1. عشوائية ترتيب الخيارات (quiz-flow)

- [x] 1.1 في `app.py`: استيراد `random` في أعلى الملف
- [x] 1.2 في مسار `/quiz`: بعد `load_questions()`، خلط ترتيب عناصر `q["options"]` لكل سؤال باستخدام `random.shuffle` على قائمة `items()` ثم إعادة بناء القاموس
- [x] 1.3 التحقق: تحميل صفحة `/quiz` مرتين والتأكد من اختلاف ترتيب الخيارات في سؤال واحد على الأقل
- [x] 1.4 التحقق: إرسال إجابة صحيحة والتأكد من أن التصحيح يعمل بشكل صحيح رغم الخلط
- [x] 1.5 التحقق: استئناف اختبار محفوظ والتأكد من أن الإجابات المحفوظة تُعبّأ في الخيارات الصحيحة رغم اختلاف الترتيب

## 2. التحقق النهائي

- [x] 2.1 تشغيل `openspec validate shuffle-options`
- [x] 2.2 تشغيل `openspec validate --all`
- [x] 2.3 أرشفة التغيير: `openspec archive shuffle-options`
