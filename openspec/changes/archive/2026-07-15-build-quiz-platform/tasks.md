## 1. بنك الأسئلة (questions.json)

- [x] 1.1 استخراج جميع أسئلة MCQ من `content.text` وتحويلها إلى بنية JSON (id, section, question, code, options, correct_answer)
- [x] 1.2 تحديد الإجابات الصحيحة يدويًا لكل سؤال بناءً على المحتوى التقني
- [x] 1.3 التحقق من أن العدد الإجمالي 95 سؤال (المحتوى الفعلي يحتوي على 95 سؤال MCQ، وليس 110 كما ذُكر في الـ proposal) وأن الأسئلة العملية مستبعدة
- [x] 1.4 التحقق: فتح `questions.json` والتأكد من صحة البنية ووجود `correct_answer` لكل سؤال

## 2. البنية الأساسية للمشروع

- [x] 2.1 إنشاء `requirements.txt` يحتوي على `flask`
- [x] 2.2 تثبيت التبعيات: `pip install -r requirements.txt`
- [x] 2.3 إنشاء مجلدات `templates/` و `static/`
- [x] 2.4 التحقق: `python -c "import flask"` يعمل بدون خطأ

## 3. خادم Flask (app.py)

- [x] 3.1 إنشاء `app.py` مع إعداد Flask و secret_key للجلسات
- [x] 3.2 تنفيذ مسار `/` (GET): عرض صفحة تسجيل الدخول
- [x] 3.3 تنفيذ مسار `/` (POST): استقبال الاسم، حفظه في الجلسة، توجيه إلى `/quiz`
- [x] 3.4 تنفيذ مسار `/quiz` (GET): تحميل الأسئلة من `questions.json` وعرضها (مع منع الوصول بدون جلسة)
- [x] 3.5 تنفيذ مسار `/submit` (POST): استقبال الإجابات، التصحيح، حساب النتيجة، حفظها في `results.json`
- [x] 3.6 تنفيذ مسار `/result` (GET): عرض النتيجة ومراجعة الإجابات الخاطئة (مع منع الوصول بدون جلسة)
- [x] 3.7 تنفيذ مسار `/results` (GET): عرض جميع النتائج من `results.json`
- [x] 3.8 تنفيذ مسار `/logout`: مسح الجلسة وتوجيه إلى `/`
- [x] 3.9 التحقق: تشغيل `python app.py` وفتح `http://localhost:5000` في المتصفح

## 4. الواجهة الأمامية (Templates)

- [x] 4.1 إنشاء `templates/login.html`: حقل الاسم + زر "ابدأ الاختبار" + دعم RTL
- [x] 4.2 إنشاء `templates/quiz.html`: عرض الأسئلة مع الخيارات (radio buttons) + شريط التقدم + زر "إنهاء الاختبار"
- [x] 4.3 إنشاء `templates/result.html`: عرض النتيجة + النسبة المئوية + رسالة تقييم + مراجعة الأخطاء
- [x] 4.4 إنشاء `templates/results.html`: جدول نتائج الطلاب
- [x] 4.5 التحقق: تصفح كل صفحة والتأكد من ظهور المحتوى بالعربية واتجاه RTL

## 5. التنسيق والتفاعل (CSS + JS)

- [x] 5.1 إنشاء `static/style.css`: تنسيق موحد، خط عربي، ألوان، تصميم متجاوب، RTL
- [x] 5.2 إنشاء `static/script.js`: عد الأسئلة غير المُجابة وعرض تنبيه بالعربية قبل الإرسال
- [x] 5.3 التحقق: فتح المنصة من متصفح الجوال والتأكد من التجاوب

## 6. التحقق النهائي الشامل

- [x] 6.1 اختبار تدفق كامل: تسجيل دخول → إجابة على الأسئلة → إنهاء → عرض النتيجة → مراجعة الأخطاء
- [x] 6.2 اختبار منع الوصول: محاولة فتح `/quiz` و `/result` بدون تسجيل دخول → توجيه إلى `/`
- [x] 6.3 اختبار النتائج: فتح `/results` والتأكد من ظهور النتائج المحفوظة
- [x] 6.4 اختبار عدم التكرار: إعادة تحميل `/result` والتأكد من عدم تكرار النتيجة
- [x] 6.5 التحقق النهائي: `openspec validate --all` يجب أن يمر بدون أخطاء
