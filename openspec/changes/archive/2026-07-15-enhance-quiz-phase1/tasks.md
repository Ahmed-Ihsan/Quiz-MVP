## 1. المؤقت الزمني (quiz-timer)

- [x] 1.1 إضافة متغير `QUIZ_TIMER_MINUTES = 30` في أعلى `app.py` (قابل للتعطيل بالضبط على 0)
- [x] 1.2 في مسار `/quiz`: حفظ `quiz_start_time` (طابع زمني Unix) في الجلسة عند أول فتح، وتمرير `timer_minutes` إلى القالب
- [x] 1.3 في `templates/quiz.html`: إضافة عنصر المؤقت في أعلى الصفحة (يظهر فقط إذا `timer_minutes > 0`)
- [x] 1.4 في `static/script.js`: تنفيذ العد التنازلي كل ثانية بصيغة "دقائق:ثوانٍ" بالعربية، وتحويل اللون للأحمر عند أقل من دقيقة، واستدعاء `form.submit()` عند الوصول إلى 00:00
- [x] 1.5 التحقق: فتح `/quiz` وظهور المؤقت + العد التنازلي + تغير اللون في الدقيقة الأخيرة (استخدام قيمة صغيرة للاختبار)

## 2. منع الغش الأساسي (anti-cheat)

- [x] 2.1 في `static/script.js`: تعطيل `copy` و`paste` و`cut` على صفحة `/quiz` (منع الانتشار ومنع الإدراج)
- [x] 2.2 في `static/script.js`: تعطيل قائمة السياق (`contextmenu`) على صفحة `/quiz`
- [x] 2.3 في `static/script.js`: كشف `visibilitychange` و`blur` — عرض تنبيه بالعربية وزيادة عداد `tabSwitches`
- [x] 2.4 في `templates/quiz.html`: إضافة حقل مخفي `tab_switches` يُحدّث من JavaScript ويُرسل مع النموذج
- [x] 2.5 في `templates/quiz.html`: إضافة زر "ملء الشاشة" يستدعي `requestFullscreen()` وزر للخروج
- [x] 2.6 التحقق: محاولة نسخ/لصق → ممنوع؛ تبديل التبويب → تنبيه + زيادة العداد؛ الزر الأيمن → لا قائمة

## 3. تمديد تخزين النتائج (results-storage)

- [x] 3.1 في مسار `/submit`: حساب `time_spent_seconds` من `quiz_start_time` في الجلسة
- [x] 3.2 في مسار `/submit`: قراءة `tab_switches` من النموذج
- [x] 3.3 في مسار `/submit`: بناء `wrong_question_ids` (مصفوفة معرّفات الأسئلة الخاطئة) أثناء التصحيح
- [x] 3.4 تمديد سجل النتيجة المحفوظ في `results.json` بالحقول الجديدة مع الحفاظ على الحقول القديمة
- [x] 3.5 في دالة `load_results`: ضمان توافق عكسي — استخدام `r.get("field", default)` للحقول الجديدة المفقودة في السجلات القديمة
- [x] 3.6 التحقق: إجراء اختبار كامل + فتح `results.json` والتأكد من وجود الحقول الجديدة + تحميل سجل قديم بدون خطأ

## 4. تصدير النتائج إلى CSV (results-storage)

- [x] 4.1 في `app.py`: إضافة مسار `/export` يستخدم وحدة `csv` المدمجة لتوليد CSV من `results.json`
- [x] 4.2 كتابة BOM (`\ufeff`) في بداية الملف لضمان ظهور العربية في Excel
- [x] 4.3 رؤوس الأعمدة بالعربية: الاسم، الإجابات الصحيحة، الإجمالي، النسبة المئوية، التاريخ، الوقت المستغرق (ث)، تبديلات التبويب
- [x] 4.4 إرجاع الملف عبر `Response` مع `mimetype='text/csv'` و`Content-Disposition: attachment; filename="results.csv"`
- [x] 4.5 في `templates/results.html`: إضافة زر "تصدير CSV" يربط بـ `/export`
- [x] 4.6 التحقق: فتح `/export` → تنزيل ملف → فتحه في Excel/محرر نصوص والتأكد من ظهور العربية بشكل صحيح

## 5. لوحة الإحصائيات (stats-dashboard)

- [x] 5.1 في `app.py`: إضافة مسار `/stats` يحسب من `results.json`: عدد المشاركين، المتوسط، أعلى/أدنى نتيجة، نسبة النجاح (≥50%)
- [x] 5.2 حساب أصعب الأسئلة: لكل سؤال، نسبة الخطأ = (عدد من أخطأ ÷ إجمالي) × 100، باستخدام `wrong_question_ids`؛ ترتيب تنازلي وأخذ أعلى 5
- [x] 5.3 حساب توزيع الدرجات: ممتاز (≥90%)، جيد (70-89%)، متوسط (50-69%)، ضعيف (<50%) — عدد الطلاب في كل فئة
- [x] 5.4 إنشاء `templates/stats.html`: عرض الملخص + قائمة أصعب الأسئلة + التوزيع، كله بالعربية مع RTL
- [x] 5.5 في `templates/stats.html`: رابط "عرض كل النتائج" → `/results`
- [x] 5.6 في `templates/results.html`: رابط "عرض الإحصائيات" → `/stats`
- [x] 5.7 التحقق: فتح `/stats` بعد وجود نتائج → ظهور كل الأقسام؛ فتحها بدون نتائج → رسالة "لا توجد نتائج بعد"

## 6. التنسيق (style.css)

- [x] 6.1 تنسيق المؤقت (موضع ثابت أعلى الصفحة، خط واضح، لون أحمر عند التحذير)
- [x] 6.2 تنسيق لوحة الإحصائيات (بطاقات الملخص، قائمة أصعب الأسئلة، أشرطة التوزيع)
- [x] 6.3 تنسيق زر التصدير وزر ملء الشاشة (بالأزرار الموجودة بصريًا)
- [x] 6.4 التحقق: التأكد من التجاوب على الجوال والحاسوب لكل العناصر الجديدة

## 7. التحقق النهائي الشامل

- [x] 7.1 اختبار تدفق كامل: دخول → اختبار مع مؤقت → تبديل تبويب (تنبيه) → إنهاء يدوي → نتيجة → فتح `/results` → تصدير CSV → فتح `/stats`
- [x] 7.2 اختبار انتهاء الوقت: ضبط `QUIZ_TIMER_MINUTES` لقيمة صغيرة (مثل 1) والانتظار → إرسال تلقائي → نتيجة
- [x] 7.3 اختبار تعطيل المؤقت: ضبط `QUIZ_TIMER_MINUTES = 0` → لا يظهر المؤقت ولا إرسال تلقائي
- [x] 7.4 اختبار توافق عكسي: إضافة سجل قديم بدون الحقول الجديدة إلى `results.json` → فتح `/results` و `/stats` و `/export` بدون خطأ
- [x] 7.5 التحقق النهائي: `openspec validate --all` يجب أن يمر بدون أخطاء
