## 1. تخزين تفاصيل الإجابات (results-storage)

- [x] 1.1 إضافة جدول `answer_details` في `init_db()` بـ `CREATE TABLE IF NOT EXISTS` مع كل الأعمدة المطلوبة
- [x] 1.2 إضافة دالة `save_answer_details(name, questions, answers_map, date_str)` تُدرج سجلًا لكل سؤال
- [x] 1.3 إضافة دالة `load_answer_details(name, date_str)` تُحمّل تفاصيل طالب محدد مرتبة حسب `question_id`
- [x] 1.4 التحقق: استدعاء الدوال والتأكد من حفظ التفاصيل في `quiz.db`

## 2. تعديل مسار الإرسال (/submit)

- [x] 2.1 بناء `user_answers_map` من بيانات النموذج لكل سؤال
- [x] 2.2 استدعاء `save_answer_details` بعد `save_results` عند حفظ النتيجة
- [x] 2.3 إزالة `wrong_answers` من `_pending_results` — الطالب لا يرى التفاصيل
- [x] 2.4 إزالة متغير `wrong_answers` غير المستخدم
- [x] 2.5 التحقق: إرسال اختبار تجريبي والتأكد من حفظ 95 سجل في `answer_details`

## 3. إخفاء الإجابات عن الطالب (web-ui)

- [x] 3.1 تعديل `result()`: عدم تمرير `wrong_answers` إلى `render_template`
- [x] 3.2 تعديل `templates/result.html`: إزالة قسم "مراجعة الإجابات الخاطئة" بالكامل
- [x] 3.3 التحقق: صفحة النتيجة تعرض النتيجة فقط بدون مراجعة

## 4. لوحة مراجعة الإجابات للأدمن (admin-answers-review)

- [x] 4.1 إضافة مسار `/admin/answers` يعرض كل الطلاب المكتملين من `results` مع `LEFT JOIN` للتحقق من وجود تفاصيل
- [x] 4.2 إضافة مسار `/admin/answers/detail?name=...&date=...` يعرض تفاصيل طالب محدد
- [x] 4.3 إنشاء `templates/admin_answers.html`: قائمة الطلاب + تفاصيل الإجابات
- [x] 4.4 إضافة رابط "إجابات الطلاب" في شريط `admin.html` العلوي
- [x] 4.5 إضافة أنماط CSS في `admin.css` لبطاقات الإجابات
- [x] 4.6 التحقق: الطلاب القدامى بدون تفاصيل يظهرون بعلامة `—`، الطلاب الجدد برابط مراجعة

## 5. التحقق النهائي

- [x] 5.1 تشغيل الخادم والتأكد من عدم وجود أخطاء
- [x] 5.2 التحقق من أن صفحة النتيجة لا تعرض الإجابات للطالب
- [x] 5.3 التحقق من أن صفحة `/admin/answers` تعرض كل الطلاب
- [x] 5.4 التحقق من أن صفحة `/admin/answers/detail` تعرض تفاصيل الإجابات
- [x] 5.5 التحقق من المسارات محمية بكلمة مرور الأدمن
