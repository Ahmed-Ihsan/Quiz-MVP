## 1. تمديد التخزين (results-storage)

- [x] 1.1 في `load_results`: إضافة `setdefault("status", "completed")` للسجلات القديمة (توافق عكسي)
- [x] 1.2 إضافة دالة `generate_resume_code()` تولّد رمز 6 أحرف من أبجدية غير ملتبسة (A-Z بدون O/I، 2-9 بدون 0/1) باستخدام `secrets`، مع تحقق التفرّد
- [x] 1.3 إضافة دالة `find_in_progress(name)` تُرجع أحدث سجل `in_progress` لاسم معين
- [x] 1.4 إضافة دالة `find_by_resume_code(code)` تُرجع سجل التقدم بـ `resume_code`
- [x] 1.5 إضافة دالة `upsert_progress(record)` تُحدّث سجل تقدم موجود أو تضيفه
- [x] 1.6 التحقق: استدعاء الدوال مع سجلات قديمة وجديدة والتأكد من التوافق العكسي

## 2. مسار الحفظ التلقائي (/save)

- [x] 2.1 إضافة مسار `POST /save` يستقبل `answers` (JSON) + `remaining_seconds` + `resume_code`
- [x] 2.2 يتحقق من وجود جلسة (اسم) قبل الحفظ؛ يرفض بدون جلسة
- [x] 2.3 يجد سجل التقدم بـ `resume_code` ويحدّث `answers` و `remaining_seconds`؛ يُرجع `{ok: true}`
- [x] 2.4 إن لم يوجد سجل تقدم (حالة نادرة): ينشئ سجل `in_progress` جديد
- [x] 2.5 التحقق: إرسال POST إلى `/save` والتأكد من تحديث السجل في `results.json`

## 3. مسار الاستئناف (/resume/<code>)

- [x] 3.1 إضافة مسار `GET /resume/<code>` يبحث عن سجل التقدم بـ `resume_code`
- [x] 3.2 إن وُجد: يضبط الجلسة (`name`) ويمرّر `saved_answers` و `remaining_seconds` و `resume_code` إلى `/quiz`
- [x] 3.3 إن لم يُوجد: يعرض رسالة عربية "رمز الاستئناف غير صحيح أو منتهي" مع رابط العودة
- [x] 3.4 التحقق: استئناف برمز صحيح يعبّئ الإجابات؛ برمز خاطئ يعرض الرسالة

## 4. تعديل تسجيل الدخول (/) والاختبار (/quiz)

- [x] 4.1 في `/`: بعد إدخال الاسم، البحث عن سجل `in_progress` بنفس الاسم
- [x] 4.2 إن وُجد: تمرير `resume_code` إلى `login.html` لعرض خيار "متابعة / بدء جديد"
- [x] 4.3 في `templates/login.html`: عرض خيار الاستئناف بالعربية عند وجود سجل غير مكتمل
- [x] 4.4 في `/quiz` لاختبار جديد: إنشاء سجل `in_progress` مع `resume_code` و `remaining_seconds = QUIZ_TIMER_MINUTES*60` و `answers = {}`
- [x] 4.5 في `/quiz` للاستئناف: تمرير `saved_answers` و `remaining_seconds` و `resume_code` إلى القالب
- [x] 4.6 التحقق: دخول باسم له سجل غير مكتمل يعرض الخيار؛ بدء جديد ينشئ سجلًا؛ متابعة يعبّئ الإجابات

## 5. تعديل الإرسال (/submit)

- [x] 5.1 قراءة `resume_code` من النموذج
- [x] 5.2 إن وُجد سجل `in_progress` بـ `resume_code`: تحديثه إلى `completed` (بدلًا من إضافة سجل جديد)
- [x] 5.3 حساب `time_spent_seconds = QUIZ_TIMER_MINUTES*60 - remaining_seconds` (محدود بـ ≥ 0) عند تفعيل المؤقت
- [x] 5.4 إن لم يوجد سجل تقدم (توافق عكسي): إنشاء سجل `completed` جديد كما يفعل الكود الحالي
- [x] 5.5 التحقق: إرسال بعد استئناف يحدّث نفس السجل إلى `completed` مع `time_spent` دقيق

## 6. تصفية مسارات القراءة

- [x] 6.1 في `/results`: تصفية السجلات إلى `status == "completed"` فقط (أو بدون `status`)
- [x] 6.2 في `/stats`: تصفية السجلات إلى `completed` فقط قبل حساب الإحصائيات
- [x] 6.3 في `/export`: تصفية السجلات إلى `completed` فقط قبل توليد CSV
- [x] 6.4 التحقق: وجود سجل `in_progress` لا يظهر في `/results` أو `/stats` أو `/export`

## 7. الواجهة الأمامية (script.js + quiz.html)

- [x] 7.1 في `quiz.html`: إضافة حقل مخفي `resume_code` (قيمته من الخادم)
- [x] 7.2 في `quiz.html`: إضافة صندوق عرض رمز الاستئناف بالعربية مع رابط `/resume/<code>`
- [x] 7.3 في `quiz.html`: إضافة مؤشر حفظ بصري ("تم الحفظ")
- [x] 7.4 في `script.js`: تعبئة أزرار الراديو من `saved_answers` عند تحميل الصفحة
- [x] 7.5 في `script.js`: المؤقت يبدأ من `remaining_seconds` (إن وُجد) بدلًا من `QUIZ_TIMER_MINUTES*60`
- [x] 7.6 في `script.js`: `setInterval` كل 15 ثانية يجمع الإجابات ويرسل POST إلى `/save`
- [x] 7.7 في `script.js`: `navigator.sendBeacon` عند `beforeunload` + حفظ عند `visibilitychange`
- [x] 7.8 التحقق: الإجابات تُحفظ دوريًا؛ إغلاق المتصفح ثم الاستئناف يعبّئ الإجابات والمؤقت يستأنف

## 8. التنسيق (style.css)

- [x] 8.1 تنسيق صندوق رمز الاستئناف (واضح، قابل للنسخ)
- [x] 8.2 تنسيق مؤشر الحفظ ("تم الحفظ" — غير مزعج)
- [x] 8.3 تنسيق خيار الاستئناف في صفحة تسجيل الدخول
- [x] 8.4 التحقق: التجاوب على الجوال والحاسوب للعناصر الجديدة

## 9. التحقق النهائي الشامل

- [x] 9.1 اختبار تدفق كامل: دخول → اختبار جديد (يظهر رمز) → إجابة 3 أسئلة → إغلاق → استئناف بالرمز → إجابات مُعبّأة + مؤقت مستأنف → إنهاء → نتيجة
- [x] 9.2 اختبار الاستئناف بالاسم: دخول بنفس الاسم → خيار "متابعة" → إجابات مُعبّأة
- [x] 9.3 اختبار "بدء جديد": اختيار بدء جديد → سجل قديم يُحذف/يُهمَل + سجل جديد
- [x] 9.4 اختبار المؤقت المتوقف: إغلاق المتصفح والمؤقت 28:45 → العودة → المؤقت 28:45 (لم ينقص)
- [x] 9.5 اختبار تصفية: سجل `in_progress` لا يظهر في `/results` أو `/stats` أو `/export`
- [x] 9.6 اختبار توافق عكسي: السجلات الـ 26 القديمة تظهر في `/results` و `/stats` بدون خطأ
- [x] 9.7 اختبار المؤقت المعطّل: `QUIZ_TIMER_MINUTES = 0` → الحفظ والاستئناف يعملان بدون حد زمني
- [x] 9.8 التحقق النهائي: `openspec validate --all` يجب أن يمر بدون أخطاء
