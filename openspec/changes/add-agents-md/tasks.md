## 1. التحقق من حقائق المشروع (قبل الكتابة)

- [x] 1.1 تأكيد مترجم Python المثبّت ونقله إلى `AGENTS.md` (Python 3.8، مسار `C:\Users\demha\AppData\Local\Programs\Python\Python38-32\python.exe`)
- [x] 1.2 تأكيد المكتبات من `requirements.txt` (Flask فقط)
- [x] 1.3 تأكيد دور `parse_questions.py` (يولّد `questions.json` من `content.text`) وعدد الأسئلة (95)
- [x] 1.4 تأكيد بنية الملفات الفعلية في الجذر + `templates/` + `static/` + `openspec/`
- [x] 1.5 تأكيد اصطلاحات الكود من `app.py` (التعليقات بالعربية، التوافق العكسي `setdefault("status","completed")`، التخزين JSON بدون DB)

## 2. كتابة AGENTS.md

- [x] 2.1 كتابة قسم "نظرة عامة" (غرض المشروع، التقنيات، القيود)
- [x] 2.2 كتابة قسم "الأوامر" (تشغيل الخادم، إعادة توليد الأسئلة، التحقق من OpenSpec) مع المترجم الصحيح
- [x] 2.3 كتابة قسم "الاصطلاحات" (العربية + RTL، JSON بدون DB، التوافق العكسي، نمط الكود الموجود)
- [x] 2.4 كتابة قسم "بنية الملفات" (دور كل ملف/دليل رئيسي)
- [x] 2.5 كتابة قسم "سير عمل OpenSpec" (مصدر الحقيقة، اقترح قبل التعديل، validate قبل الاكتمال)
- [x] 2.6 كتابة قسم "القيود" (محلي على localhost:5000، بدون قاعدة بيانات، مستخدم واحد لكل محاولة)

## 3. التحقق من الدقة

- [x] 3.1 تنفيذ كل أمر موثّق في `AGENTS.md` والتأكد أنه يعمل (تشغيل الخادم، إعادة توليد الأسئلة، `openspec validate --all`)
- [x] 3.2 مطابقة قسم بنية الملفات مع `Get-ChildItem` الفعلي
- [x] 3.3 التأكد من عدم وجود افتراضات (مثل مكتبات غير مثبّتة أو أوامر غير موجودة)

## 4. التحقق النهائي

- [x] 4.1 `openspec validate add-agents-md` يمر بدون أخطاء
- [x] 4.2 `openspec validate --all` يمر بدون أخطاء
- [x] 4.3 `openspec status --change add-agents-md` يُظهر اكتمال proposal/specs/design/tasks
- [x] 4.4 التأكد أن لا ملف كود أو قالب أو بيانات تأثر بهذا التغيير
